<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= (typeof title !== 'undefined' ? title : 'Error') %> - Toko Digital</title>
    <!-- Tailwind CSS 4 Browser -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }
        .error-container {
            text-align: center;
            max-width: 600px;
            padding: 2rem;
        }
        .error-icon {
            font-size: 5rem;
            margin-bottom: 2rem;
            opacity: 0.8;
        }
        .error-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 3rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .btn-home {
            background: rgba(255, 255, 255, 0.2);
            border: 2px solid white;
            color: white;
            border-radius: 50px;
            padding: 12px 30px;
            text-decoration: none;
            font-weight: 600;
            display: inline-block;
            margin-top: 2rem;
            transition: all 0.3s ease;
        }
        .btn-home:hover {
            background: white;
            color: #667eea;
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-card">
            <div class="error-icon">
                <i class="bi bi-exclamation-triangle"></i>
            </div>
            
            <h1 class="display-4 mb-4"><%= title || 'Oops!' %></h1>
            <p class="lead mb-4"><%= message || 'Terjadi kesalahan yang tidak terduga.' %></p>
            
            <% if (error && error.stack && process.env.NODE_ENV === 'development') { %>
                <details class="text-start mt-4">
                    <summary class="btn btn-sm btn-outline-light mb-3">Detail Error (Development)</summary>
                    <pre class="bg-dark text-light p-3 rounded small text-start"><%= error.stack %></pre>
                </details>
            <% } %>
            
            <div class="mt-4">
                <a href="/" class="btn-home">
                    <i class="bi bi-house-door me-2"></i>
                    Kembali ke Beranda
                </a>
                <a href="/admin" class="btn-home ms-3">
                    <i class="bi bi-gear me-2"></i>
                    Dashboard Admin
                </a>
            </div>
        </div>
    </div>
</body>
</html>
